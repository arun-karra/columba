auto-install-peers=false
strict-peer-dependencies=false
# Deduplicate Expo / React Native so EAS native builds see one copy of each module.
public-hoist-pattern[]=*expo*
public-hoist-pattern[]=*react-native*
public-hoist-pattern[]=*react-native-*
