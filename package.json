{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "eas:build:dev": "pnpm --filter @workspace/ios-app run eas:build:dev",
    "eas:build:sim": "pnpm --filter @workspace/ios-app run eas:build:sim",
    "eas:build:preview": "pnpm --filter @workspace/ios-app run eas:build:preview",
    "eas:build:prod": "pnpm --filter @workspace/ios-app run eas:build:prod",
    "mac:setup": "bash scripts/mac-setup.sh",
    "mac:dev": "bash scripts/mac-dev.sh",
    "mac:sim": "pnpm --filter @workspace/ios-app run eas:build:sim",
    "mac:ios": "pnpm --filter @workspace/ios-app exec expo run:ios"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
